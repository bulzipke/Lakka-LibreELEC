diff --git a/libretro/Makefile b/libretro/Makefile
index 94f5e9a..062766d 100644
--- a/libretro/Makefile
+++ b/libretro/Makefile
@@ -47,6 +47,10 @@ ifeq ($(ARCH),)
    ARCH = $(shell uname -m)
 endif
 
+ifeq ($(ARCH), aarch64)
+   FLAGS += -mno-outline-atomics
+endif
+
 ifeq ($(platform), unix)
    TARGET := $(TARGET_NAME)_libretro.so
    fpic := -fPIC
